{
  "name": "realm-token-server",
  "version": "0.1.1",
  "private": true,
  "type": "module",
  "description": "Realm credential-exchange + LiveKit token-mint server. Verifies a provider ID token, mints an opaque Realm credential (ES256), and exchanges that credential for a LiveKit access token. See packages/realm/DESIGN.md in enspyrco/tech_world.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node src/index.js",
    "test": "node --test"
  },
  "dependencies": {
    "express": "^4.19.2",
    "firebase-admin": "^12.1.0",
    "jsonwebtoken": "^9.0.2",
    "livekit-server-sdk": "^2.15.0"
  }
}
